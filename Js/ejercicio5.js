// Declarar las variables horas y minutos
let horas = 100; // Puedes cambiar este valor
let minutos = 60; // Puedes cambiar este valor

// Realizar la conversión a segundos
let segundos = (horas * 3600) + (minutos * 60);

// Mostrar los valores y el resultado en consola
console.log(`Horas: ${horas}, Minutos: ${minutos}`);
console.log(`Total en segundos: ${segundos}`);